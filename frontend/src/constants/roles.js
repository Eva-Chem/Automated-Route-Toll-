/**
 * Role Constants
 * These match exactly with backend JWT role claims.
 * Source of truth: backend/routes/auth_routes.py
 */

export const ROLES = {
  ADMIN: "admin",
  TOLL_OPERATOR: "toll_operator",
};

export default ROLES;
